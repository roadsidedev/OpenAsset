---
title: Developer guide
description: What developers can build on OpenAsset and how to build, test, integrate, and submit it.
---

# Developer guide

OpenAsset is a permissionless lending protocol for isolated markets. Developers can build new markets and integrations around ERC-20s, NFTs, tokenized real-world assets, and other assets by selecting asset, oracle, compliance, liquidation, and position adapters.

The core engine retains loan state, accounting, circuit breakers, reentrancy protection, and defensive output checks. Adapters provide bounded integrations at an explicit trust boundary. A registry badge is not a substitute for on-chain validation or independent risk assessment.

## What can you build?

| Goal | Start here |
|---|---|
| Launch an isolated market | [Create a market](/docs/guides/create-market) |
| Add a new collateral type | [Adapter system](/docs/protocol/adapter-system) and [adapter templates](/docs/guides/reference-adapters) |
| Add a price source | [Oracle adapters](/docs/protocol/oracles) and the [adapter specification](/docs/protocol/adapter-system) |
| Add eligibility rules | [Compliance boundary](/docs/protocol/compliance) |
| Add a liquidation route | [Liquidations](/docs/protocol/liquidations) |
| Represent loan ownership | [Position adapters](/docs/protocol/positions) |
| Integrate registry and market data | [Adapter registry](/docs/protocol/adapter-registry) |

## Developer journey

1. **Understand:** read the [protocol overview](/docs/protocol/overview), [adapter system](/docs/protocol/adapter-system), and [security model](/docs/protocol/security).
2. **Install:** clone the repository, install `contracts` and `sdk` dependencies, and compile locally.
3. **Choose a starting point:** use the [reference adapters](/docs/guides/reference-adapters) or generate an SDK scaffold.
4. **Implement:** follow the [adapter security model](/docs/protocol/security), including factory permissions, market isolation, fail-closed behavior, and non-upgradeable deployment.
5. **Test:** use the local [testing environment](/docs/guides/developer-quickstart#5-test) with mock assets, feeds, compliance, liquidation, failures, and gas checks.
6. **Submit:** run automated checks, record the source commit and dependencies, register metadata, and follow the [verification workflow](/docs/protocol/adapter-registry#review-meaning).

## Platform documentation map

- **Market creation:** [Create a market](/docs/guides/create-market) and [market risk boundaries](/docs/scope-and-risks).
- **Adapter development:** [Adapter system](/docs/protocol/adapter-system), [reference adapters](/docs/guides/reference-adapters), and [quick start](/docs/guides/developer-quickstart).
- **SDK and local testing:** [Developer quick start](/docs/guides/developer-quickstart) and [testing environment](/docs/guides/developer-quickstart#5-test).
- **Registry and verification:** [Adapter registry](/docs/protocol/adapter-registry), [trust model](/docs/protocol/trust-model), and [security checklist](/docs/reference/security-checklist).
- **Deployment and integration:** [Operations](/docs/guides/developer-quickstart#6-submit), [multi-chain](/docs/protocol/multi-chain), and [core contracts](/docs/protocol/core-contracts).
- **Feedback:** [Developer feedback loop](/docs/guides/developer-feedback).

## Existing page roles

The platform docs already contain conceptual pages for the adapter system, trust model, registry, oracles, liquidations, compliance, positions, and market creation. This guide does not replace them: it is the developer entry point that connects those pages into an actionable build path. The quick start provides commands; the conceptual pages explain protocol behavior and user risk.

## Local commands

```bash
cd contracts
npm install
npm run compile
npx hardhat test

cd ../sdk
npm test
npm run pack:check
```

For the complete path, continue to [Developer quick start](/docs/guides/developer-quickstart).
